# ROADMAP - skill-cli-kit

> Source of truth for current progress.

## Current Progress

**Phase**: Phase 1 - reusable skill + CLI scaffold
**Current Step**: Step 4b done; v0.1.0 public native release published; ready for third real-project validation

### Step Status

| Step | Scope | Status |
|---|---|---|
| 0 | Extract common pattern from `bvr` and `docdev` | Done |
| 1 | Create first working `skillcli` scaffold/audit/sync toolkit | Done |
| 2 | Incorporate web and LarkCLI best-practice pass | Done |
| 3 | Add update lifecycle for source-to-installed skill sync | Done |
| 4 | Add review-style audit for existing CLI skill projects | Done |
| 4a | Promote `skill-cli-kit` to a canonical `~/Project` source checkout | Done |
| 4b | Make `skill-cli-kit` its own native-install reference project | Done |
| 5 | Use on a third real skill and refine checks | Pending |

---

## Step 0 - Extract common pattern

**Goal**: Turn the proven local pattern into explicit reusable rules.

**Tasks**:
- [x] Read Bilibili skill + CLI structure.
- [x] Read docs-driven-dev skill + CLI structure.
- [x] Identify shared install, sync, doctor, and wrapper strategy.

**Acceptance**:
1. The shared boundary is captured in SPEC.
2. The generated scaffold contract is explicit.
3. The risks are logged in DECISIONS.

## Step 1 - Build first toolkit

**Goal**: Provide a small source project that can scaffold and audit future
skill-backed CLI projects.

**Tasks**:
- [x] Add `skillcli` commands: init, audit, status, sync-skill, doctor.
- [x] Add portable `skill/SKILL.md` and OpenAI metadata.
- [x] Add install/sync/check scripts.
- [x] Add focused tests.
- [x] Add project docs.

**Acceptance**:
1. `python3 -m unittest discover -s tests` passes.
2. `skillcli audit <project>` returns no errors for this project.
3. A generated sample project audits cleanly.

## Step 2 - Incorporate external and LarkCLI practice

**Goal**: Incorporate external CLI best practices and LarkCLI's agent-native
skill + CLI pattern.

**Tasks**:
- [x] Check Python argparse and packaging entrypoint documentation.
- [x] Check agent-readable CLI output patterns.
- [x] Inspect LarkCLI's public README and local Lark skills.
- [x] Add generated skill metadata for required CLI bins.
- [x] Add JSON output for status and doctor.

**Acceptance**:
1. Generated skills advertise the required CLI bin.
2. Status, doctor, and audit can be consumed as structured JSON.
3. Lark-inspired shared/domain skill ideas are captured in references.

## Step 3 - Add update lifecycle

**Goal**: Let source checkout updates propagate to installed skills with one
repeatable command.

**Tasks**:
- [x] Define the update lifecycle in SPEC.
- [x] Add `skillcli update <project>`.
- [x] Scaffold `scripts/update_cli.sh` for generated projects.
- [x] Add regression tests for install/test/check/sync/check.

**Acceptance**:
1. Update runs project checks before syncing the installed skill.
2. Update can emit structured JSON.
3. Generated projects include a project-local update script.

## Step 4 - Review existing CLI skill projects

**Goal**: Let `skillcli audit` inspect already-existing skill + CLI projects and
point out both required gaps and optimization opportunities.

**Tasks**:
- [x] Add finding categories and recommendations.
- [x] Check existing project scripts, metadata, tests, docs, and update lifecycle.
- [x] Validate against `bilibili-video-reading`, `docs-driven-dev`, and `skill-cli-kit`.

**Acceptance**:
1. Audit output distinguishes errors from optimization warnings.
2. Audit JSON contains actionable recommendations.
3. Existing CLI skill projects can be reviewed without requiring they were
   generated by `skillcli`.

## Step 4a - Normalize source checkout

**Goal**: Make `skill-cli-kit` itself follow the same one-project-folder rule it
teaches other CLI-backed skills.

**Tasks**:
- [x] Move or copy the source checkout to `/Users/chihoyo/Project/skill-cli-kit`.
- [x] Initialize `/Users/chihoyo/Project/skill-cli-kit` as its own git repository.
- [x] Refresh `.venv/bin/skillcli`, global/user-facing `skillcli`, and installed skill-local wrappers.
- [x] Document the canonical source checkout rule in SPEC, ARCHITECTURE, and DECISIONS.

**Acceptance**:
1. `skillcli -v` works from a fresh shell and resolves to the canonical checkout.
2. `skillcli doctor --json` reports the canonical source root.
3. `/Users/chihoyo/Project/skill-cli-kit` has its own git repository.
4. `docdev audit /Users/chihoyo/Project/skill-cli-kit` and `skillcli audit /Users/chihoyo/Project/skill-cli-kit --json` pass without errors.

## Step 4b - Native-install reference shape

**Goal**: Make `skill-cli-kit` itself demonstrate the native release install
shape it should later teach other skill-backed CLI projects.

**Tasks**:
- [x] Add release packaging and remote installer scripts.
- [x] Add `skillcli native-update` and `skillcli uninstall`.
- [x] Document native install vs source checkout install in SPEC, ARCHITECTURE,
  README, skill, and DECISIONS.
- [x] Create a docs-driven change packet for the native install requirement.

**Acceptance**:
1. `scripts/package_release.sh` produces artifact, checksum, manifest, and
   installer assets.
2. `scripts/install_remote.sh` can install from a local `file://` release asset
   directory into a temporary native layout.
3. GitHub Releases installer can install `skillcli 0.1.0` into a temporary
   native layout.
4. Tests, `docdev audit`, and `skillcli audit` pass.

## Step 5 - Validate on a third real skill

**Goal**: Use `skillcli` outside its own project and refine only the checks that
real usage proves useful.

**Tasks**:
- [ ] Pick a future reusable behavior to CLI-ify.
- [ ] Run `skillcli init` for it.
- [ ] Replace placeholder commands with real deterministic helpers.
- [ ] Run audit before sync.

**Acceptance**:
1. The generated project can be used from an unrelated working directory.
2. The installed skill-local wrapper finds the source checkout.
3. Audit findings are useful and not noisy.
